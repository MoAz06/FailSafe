{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "timeout": 12,
            "command": "( python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/slopguard.py\" || python \"${CLAUDE_PLUGIN_ROOT}/hooks/slopguard.py\" ) 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
