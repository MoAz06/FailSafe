{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "timeout": 12,
            "command": "( python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/failsafe.py\" || python \"${CLAUDE_PLUGIN_ROOT}/hooks/failsafe.py\" ) 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
